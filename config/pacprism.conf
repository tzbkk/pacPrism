# pacPrism Configuration File
# Lines starting with # are comments

# Upstream mirror address for package requests
upstream=ftp.debian.org

# Cache directory for storing package files
cache_dir=./cache
