# pacPrism Configuration File
# Lines starting with # are comments

# Upstream mirror address for package requests
upstream=ftp.debian.org

# Server listening port (can be overridden by command-line argument)
port=9001

# Server bind address (0.0.0.0 for all interfaces)
bind_address=0.0.0.0
